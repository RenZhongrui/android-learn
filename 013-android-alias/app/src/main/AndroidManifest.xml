<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.learn.alias">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_alias"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.learn.alias.Main2Activity"></activity>
<!--        <activity-alias
            android:name="com.learn.alias.AliasActivity"
            android:enabled="false"
            android:icon="@mipmap/ic_launcher_alias"
            android:label="Alias"
            android:targetActivity="com.learn.alias.Main2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>-->
    </application>

</manifest>