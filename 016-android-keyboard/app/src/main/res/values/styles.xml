<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- SecurityKeyboard theme. -->
    <style name="SecurityKeyboardTheme" parent="@android:style/Theme.Dialog">
        <!-- 设置未浮动窗口 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 设置无边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 设置无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置无阴影 -->
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <!-- 设置完全透明 -->
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 设置动画 -->
        <item name="android:windowAnimationStyle">@style/SecurityKeyboardAnimation</item>
    </style>

    <style name="SecurityKeyboardAnimation" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/agreesdk_dialog_bottom_enter</item>
        <item name="android:windowExitAnimation">@anim/agreesdk_dialog_bottom_exit</item>
    </style>

    <style name="KeyBroadKeyStyle">
        <item name="android:textSize">22dp</item>
        <item name="android:clickable">true</item>
        <item name="android:background">@drawable/agreesdk_keybroad_key_selector</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#686868</item>
        <item name="android:layout_marginTop">3dp</item>
        <item name="android:layout_marginBottom">3dp</item>
        <item name="android:layout_marginRight">6dp</item>
    </style>

    <style name="KeyBroadKeyStyleItem">
        <item name="android:textSize">22dp</item>
        <item name="android:clickable">true</item>
        <item name="android:background">@drawable/agreesdk_keybroad_key_selector</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#686868</item>
    </style>
</resources>
